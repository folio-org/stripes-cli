import {
  add,
  descriptor,
  disable,
  discover,
  enable,
  filter,
  install,
  list,
  perms,
  pull,
  remove,
  update,
  view,
} from './mod/index.js';

export default {
  command: 'mod <command>',
  describe: 'Commands to manage UI module descriptors',
  builder: yargs => yargs.command([
    add,
    descriptor,
    disable,
    discover,
    enable,
    filter,
    install,
    list,
    perms,
    pull,
    remove,
    update,
    view,
  ]),
  handler: () => {},
};
